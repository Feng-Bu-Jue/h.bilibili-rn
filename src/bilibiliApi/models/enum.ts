export enum Enum_RankType {
    day,
    week,
    month
}

export enum Enum_DrawCategory {
    //for doc
    all,
    illustration,
    comic,
    other,
    //for photo
    sifu,
    cos
}

export enum Enum_Biz {
    all,
    draw,
    photo,
    daily
}
